<mvc:View 
    controllerName="demofioriapp.controller.Overview"  
    xmlns:mvc="sap.ui.core.mvc" 
    displayBlock="true"
    xmlns="sap.m">
    <Page id="overview" title="{i18n>pageTitle}">
        <landmarkInfo>
            <PageAccessibleLandmarkInfo 
                rootRole="Region"
                rootLabel="{i18n>OverviewRootLabel}"
                contentRole="Main"
                contentLabel="{i18n>OverviewContentLabel}"
                headerRole="Banner"
                headerLabel="{i18n>OverviewHeaderLabel}">
            </PageAccessibleLandmarkInfo>
        </landmarkInfo>
        <headerContent>
            <Button 
                icon="sap-icon://hello-world"
                press=".onOpenDialog"/>
        </headerContent>
        <content>
           <mvc:XMLView viewName="demofioriapp.view.Panel1"/>
           <mvc:XMLView viewName="demofioriapp.view.InvoiceList"/>
        </content>
    </Page>
</mvc:View>
